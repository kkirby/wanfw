# Progress log

One line per task: id, status, date, deviations. See `docs/wanfw-implementation-plan.md` for task definitions.

- **T0.1** done 2026-07-11. Monorepo bootstrap: pnpm workspaces, root tsconfig.base.json (strict/ESM/NodeNext/ES2022), vitest, placeholder packages for all 13 workspace members. `pnpm -r build` and `pnpm -r test` pass.
- **T0.2** done 2026-07-11. core-schemas: envelope/framework/service/plugin-config JSON Schemas (2020-12), capability taxonomy (§12.1 ∪ §6.6), canonical JSON serializer, powerful-projection function with fixed sha256 test vectors, atomic-write util (temp+rename+fsync), name constants module. 34 unit tests green.
- **T0.3** done 2026-07-11. Compose stack (`deploy/docker-compose.yml`) matches Appendix A mounts/networks/isolation. Three Dockerfiles (tier1/orchestrator/pluginhost) build on `node:22-bookworm-slim`, non-root users. Verified locally on macOS + Docker Desktop: `docker compose config` validates, all three images build, stack boots and stays up with placeholder processes (orchestrator/pluginhost log heartbeat JSON, tier1 serves HTTP on 8443). Deviation: tier1 and orchestrator are not yet `read_only` rootfs — that hygiene pass is explicitly T6.3's job per the plan; Appendix A itself only marks tier1/pluginhost read-only, so this matches spec, not a deviation.
- **T0.4** in progress 2026-07-11. CI workflow authored at `.github/workflows/ci.yml` (install+build+test job, image-build job, integration job running `test/integration/run.sh`, currently a no-op placeholder per plan). **Deviation:** this repository has no configured git remote in this environment, so "CI green on main" could not be verified by actually running GitHub Actions. Validated locally instead: `pnpm -r build`, `pnpm -r test`, and `docker compose build`/`up` all succeed on this machine, which is what the CI job steps run. Revisit once a remote exists — push and confirm the Actions run goes green.
- **T1.1** done 2026-07-11. Orchestrator process skeleton: heartbeat loop writing `wanfw_status/framework.json` atomically every 10s, two UDS HTTP servers (status + admin), tiny JSON route dispatcher (`JsonUdsRouter`), stale-socket unlink on boot, structured JSON logging. Verified live in the real container: both sockets answer `curl --unix-socket` via `docker exec`, heartbeat file updates on disk.
- **T1.2** done 2026-07-11. Status socket routes: `GET /status`, `GET /status/services/:id`, `GET /schema` (404 stub), `GET /approvals/pending` (empty), `POST /validate` (501 stub, asserted not to mutate heartbeat state), `POST /nudge`. Allowlist test (`status-socket.test.ts`) enumerates the route table against `STATUS_SOCKET_ROUTE_ALLOWLIST` and fails on drift — this is the permanent enforcement of invariant #4; do not weaken it.
- **T1.3** done 2026-07-11. `wanfwctl` skeleton: `packages/wanfwctl` produces `wanfwctl-inner` (commander) speaking JSON-HTTP to `admin.sock`; `deploy/wanfwctl` host wrapper runs `docker exec -i wanfw-orchestrator wanfwctl-inner "$@"`. `status` command implemented end to end. Exit codes (`EXIT_CODES`) encoded as constants matching spec §11 / plan interpretation 7 and asserted in tests. Verified live: `./deploy/wanfwctl status` round-trips against the real container and prints the heartbeat JSON; exit code 0 confirmed on success, non-zero confirmed when the orchestrator container is stopped.
- **T1.4** done 2026-07-11. Tier1 Next.js app (App Router, `output: "standalone"`, Mantine, TypeScript strict): login page + argon2id-verified server action, sessions + admin password hash in SQLite (`wanfw_tier1state`), sliding-window login rate limit persisted in the same DB, HttpOnly/SameSite=Strict session cookie, middleware guarding everything except `/login`/static, `lib/orch.ts` UDS client marked `server-only`, dashboard server component reading `GET /status` plus a client polling loop through an `/api/status` route handler proxy. Dev bootstrap `scripts/seed-admin.mjs` seeds the admin hash until `wanfwctl init` (T5.3) exists. **Deviation:** `ShellLayout` (the authenticated app shell) must be a client component (`"use client"`) — Mantine's `AppShell.Main`/`AppShell.Header` compound sub-properties don't survive being read from a Server Component in this Next 15 + React 19 + Mantine 7 combination (produced `Element type is invalid: ... undefined`); the actual page content underneath remains server-rendered as normal. Verified live end to end in the real container: seeded the admin hash via `docker exec`, logged in through the real form-action protocol against `http://localhost:8443`, got the HttpOnly session cookie, dashboard rendered the live orchestrator heartbeat (`{"phase":"pending-init",...}`) fetched over the real `orch-status.sock`, `/api/status` returned the same JSON, and 11 rapid wrong-password attempts correctly triggered "Too many attempts". Container runs with `read_only: true` + tmpfs `/tmp` per compose.
- **T1.5** done 2026-07-11. Integration harness (`test/integration/run.sh`) replaces the T0.4 no-op stub: brings the compose stack up, asserts §12.5 negative properties via `docker exec`/`docker inspect` (tier1 cannot stat the Docker socket or reach admin.sock; tier1/pluginhost mount none of `wanfw_state`/`wanfw_secrets`/`wanfw_rpc_plugin`; orchestrator's only network interface is `lo`; orchestrator is the one Docker-socket holder; both orchestrator sockets accept connections), tears down, and exits nonzero on any failure. CI's integration job now just runs this script. **Verified the negative case, not just the positive one** (plan explicitly requires this): deliberately added a `wanfw_state` bind into the tier1 service in a scratch copy of `docker-compose.yml`, reran the harness, confirmed it printed `[FAIL] tier1 has volume wanfw_wanfw_state mounted` and exited 1, then reverted and confirmed a clean pass again.

**Gate M0: reached 2026-07-11.** All of T1.1-T1.5 done; compose stack boots (tier1 login works, orchestrator heartbeat writes/serves, pluginhost idle placeholder logs); `./deploy/wanfwctl status` round-trips against the real container; §12.5 negative assertions pass locally via `test/integration/run.sh` (verified both the positive case and that it actually catches a deliberately introduced violation). CI workflow is authored and exercises the same steps, but has not been observed green on an actual GitHub Actions run since this environment has no git remote configured (see T0.4 deviation note).

Next up per the plan: Phase 2 (M1) -- SQLite state store (T2.1), Ed25519 signing key (T2.2), audit log (T2.3), plugin manifest schema/loader (T2.4), trust flow (T2.5), pluginhost supervisor (T2.6), host API + grant enforcement (T2.7), plugin SDK (T2.8), echo test plugin (T2.9), tier1 plugin management UI (T2.10).
- **T2.1** done 2026-07-11. SQLite state store (`StateStore` in `packages/orchestrator/src/state-store`): better-sqlite3 WAL, migration runner tracked via `schema_migrations`, tables for `trust_records`, `grants`, `approvals`, `ipam_ranges`/`ipam_allocations`, `plugin_kv`, `journal`, `meta`. Wired into `main.ts` at boot. 9 unit tests (CRUD + migration idempotency). Verified live: `docker exec` shows `/data/state/state.sqlite3` created at mode 0600 inside the real container.
- **T2.2** done 2026-07-11. Ed25519 signing key (`SigningKeyManager`): first-boot keygen, PKCS8 PEM at 0600 in `wanfw_state`; `sign`/`verify` over arbitrary canonical payloads; `rotate()` + `reSignAll(store)` re-sign every live trust/grant/approval record; `importFrom()` replaces custody. Admin socket routes `GET /key`, `POST /key/rotate`, `POST /key/import`. `wanfwctl key show/rotate/import` (import reads PKCS8 PEM from stdin only, never argv). 9 unit tests including the key rotation-staleness contract (old sig verifies against the old public key, fails against the new one) and re-signing round-trip. Verified live: rotated the real container's key via `./deploy/wanfwctl key rotate` and confirmed `key show` persists the new key.
- **T2.3** done 2026-07-11. Audit log (`AuditLog`): append-only JSONL in `wanfw_state`, `prevHash` hash chain, Ed25519 checkpoint signature every 100 entries and on every security-relevant type (`plugin.trust/untrust`, `grant.create/revoke`, `plan.approve/revoke`, `key.rotate/import`, `powerful.execute`, `secret.set/unset`). Admin socket now writes an entry on every mutation (currently `key.rotate`/`key.import`); `GET /audit` and `POST /audit/verify` routes; `wanfwctl audit tail [--verify]`. Fixed a reference-staleness bug during review: `AuditLog` takes a signing-key getter, not a captured instance, since `key import` swaps the live `SigningKeyManager` object (`rotate()` mutates in place, `importFrom()` doesn't). 8 unit tests including the required tamper test (flip a byte in a historical entry -> `verify()` fails loudly with the failing seq). Verified live: rotated the key in the real container (which audit-logs `key.rotate` with a checkpoint signature), ran `audit tail --verify` clean (exit 0), then hand-tampered the JSONL file inside the container and confirmed `audit tail --verify` reports `TAMPER DETECTED at seq 1: hash mismatch` and exits 6 (refused).
- **T2.4** done 2026-07-11. Manifest schema + loader in `core-schemas`: JSON Schema for the plugin manifest (§6.2) including `capabilities[].enforcement: "declared"`; `isFrameworkApiCompatible()` (hand-rolled caret-range check against `FRAMEWORK_API_VERSION`, deliberately not a full semver dependency since the manifest format only ever uses `^X.Y[.Z]`); `resolveScopeTemplates()` for `${framework.domain}`-style scope templates (resolved at grant time, per the spec note that the *resolved* scope is what gets recorded and signed); `loadManifest()`/`validateManifest()` loader. Fixture bundles (valid / invalid / template-bearing) under `src/__fixtures__/`, 18 unit tests covering schema validity, framework-API compat edge cases, and template resolution in nested scopes.
