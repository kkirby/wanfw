#!/usr/bin/env bash
# Integration harness (T1.5 wires real §12.5 assertions here). Committed now
# as a no-op so the CI job structure exists from T0.4 onward.
set -euo pipefail
echo "[integration] no assertions wired yet (see T1.5 in docs/wanfw-implementation-plan.md)"
